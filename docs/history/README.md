# History folder

`worklog.txt` is a **local-only** append log (optional). It is listed in `.gitignore` so routine entries are not committed. Meaningful project changes belong in normal commits touching code, contracts, and tests—not one-line timestamp appends.

Optional local automation can live under `scripts/git/`; that directory is listed in `.gitignore` and is not part of the remote repository.
